[package]
name = "omen-fan"
version = "0.5.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_derive = "1.0"
toml = "0.8"
nix = { version = "0.29", features = ["user"] }
clap = { version = "4.5", features = ["derive"] }

[features]
acpi_ec = []
